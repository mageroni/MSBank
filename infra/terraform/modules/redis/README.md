# redis

Azure Cache for Redis (default: Standard C1, TLS-only). Optional Private
Endpoint disables public access.

Primary access key is exported as a sensitive output for the root composition
to store in Key Vault.
